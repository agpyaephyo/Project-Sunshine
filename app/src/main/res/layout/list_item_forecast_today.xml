<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View" />
        <variable
            name="weatherStatus"
            type="net.aung.sunshine.data.vos.WeatherStatusVO" />
    </data>

    <RelativeLayout
        android:id="@+id/rl_list_forecast_today_root"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/primary"
        android:foreground="?android:attr/selectableItemBackground"
        android:paddingBottom="@dimen/margin_medium_2"
        android:paddingLeft="@dimen/margin_xxlarge"
        android:paddingRight="@dimen/margin_medium_2"
        android:paddingTop="@dimen/margin_medium_2">

        <LinearLayout
            android:id="@+id/ll_forecast_desc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            >

            <ImageView
                android:id="@+id/iv_status_art"
                android:layout_width="@dimen/today_weather_icon_dimen"
                android:layout_height="@dimen/today_weather_icon_dimen"
                android:adjustViewBounds="true"
                tools:src="@drawable/art_clouds"
                android:layout_marginTop="@dimen/margin_medium_2"
                />

            <TextView
                android:id="@+id/tv_status"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-condensed"
                android:gravity="center_horizontal"
                android:textColor="@android:color/white"
                android:textAppearance="?android:textAppearanceLarge"
                tools:text="Clouds"
                android:text="@{weatherStatus.weather.description}"
                />

        </LinearLayout>

        <TextView
            android:id="@+id/tv_max_temperature"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:fontFamily="sans-serif-light"
            android:textColor="@android:color/white"
            android:textSize="@dimen/text_max_temperature"
            android:text="@{weatherStatus.temperature.maxTemperatureDisplay}"
            tools:text="21" />

        <TextView
            android:id="@+id/tv_day"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-condensed"
            android:textColor="@android:color/white"
            android:textAppearance="?android:textAppearanceLarge"
            tools:text="Today, December 13"
            android:text="@{weatherStatus.dateDisplay}"
            />

        <TextView
            android:id="@+id/tv_min_temperature"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_marginBottom="@dimen/margin_medium_2"
            android:textColor="@android:color/white"
            android:textSize="@dimen/text_min_temperature"
            android:text="@{weatherStatus.temperature.minTemperatureDisplay}"
            tools:text="13" />

    </RelativeLayout>

</layout>
